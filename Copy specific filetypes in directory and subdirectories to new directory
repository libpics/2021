#https://stackoverflow.com/questions/296173/how-do-i-copy-files-with-specific-file-extension-to-a-folder-in-my-python-versi
#This code will copy files with a specific extension in a folder and all it's subfolders to a new directory
#this would be good for copying all the tiffs in the 

import os, shutil

srcDir='/Volumes/visualresources/Book Orders/Done 2021'
dest='/Volumes/visualresources/All TIFFs'

for root, dirs, files in os.walk(srcDir):
    for file in files:
        if file[-4:].lower() == '.tif':
            shutil.copy(os.path.join(root, file), os.path.join(dest, file))
